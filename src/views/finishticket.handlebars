<div class="container login">
    <div class="card col-10 cardsBorder">
      <div class="card-header card-header-dashboard-style ">
        <h2>¡Compra Finalizada!</h2>
      </div>
      <div class="card-body">
        <h5 class="card-title">Ticket / Orden</h5>
        <div class=>         
          <div class=" cardsBorder-dashboard col-4">
            
            {{#each ticket.products as |product|}}            
              <div class="card-body">  
                <p class="card-text"> {{product.idProduct.title}} </p> 
                <p class="card-text">  Cantidad:  {{product.quantity}} </p>
              </div>
            {{/each}}
            <p class="card-text"><b>TOTAL</b>  ${{totalCart}}</p>
          </div>          
        </div>
      </div>
      <a style="text-align: left" href='/'>Volver</a>
    </div>
</div>  